{
  "name": "umbraco-ai-ops",
  "owner": {
    "name": "Umbraco",
    "url": "https://github.com/umbraco/umbraco-ai-ops"
  },
  "metadata": {
    "description": "The generic engine for AI-driven issue automation across Umbraco products — orchestration, GitHub/CI ops, gated merge, cross-repo dispatch, and the self-learning loop. Product-agnostic: each consumer supplies only a build playbook + a config block. Extracted from the umbraco-mcp-ops prototype.",
    "version": "0.1.0",
    "homepage": "https://github.com/umbraco/umbraco-ai-ops"
  },
  "plugins": [
    {
      "name": "ops-setup",
      "source": "./plugins/ops-setup",
      "description": "Interactive onboarding: /umbraco-ops-setup investigates a repo (branching model from git history, CI host, release approach), confirms and fills the gaps via the question tool, then generates the consumer files — .claude/ai-ops.yml, a build-playbook scaffold, any override skills the repo needs, and the caller workflow(s). Run once per repo after installing the engine.",
      "version": "0.1.0",
      "category": "workflow",
      "keywords": [
        "setup",
        "onboarding",
        "config",
        "detection",
        "interactive",
        "scaffold"
      ]
    },
    {
      "name": "issue-loop-core",
      "source": "./plugins/issue-loop-core",
      "description": "The orchestration engine: work a backlog of ops/ready-for-ai issues to CI-green PRs. Owns the durable /goal loop, rolling cap-3 dispatch, the human-review-response phase, model selection, and stop conditions/backstops. Loads a product's build playbook via a named slot (it does not know how to build any one product). Bundles rework-loop (PR-review-event response). Repo-agnostic; requires github-ops.",
      "version": "0.1.0",
      "category": "workflow",
      "keywords": [
        "umbraco",
        "github-issues",
        "orchestration",
        "ci",
        "pull-request",
        "automation",
        "cloud-routine"
      ]
    },
    {
      "name": "github-ops",
      "source": "./plugins/github-ops",
      "description": "Shared reference for doing GitHub work two ways — locally with the `gh` CLI + `git`, and on Claude web / in scheduled routines with the GitHub MCP server (`mcp__github__*`). One operation catalog, two reference files; the other loop plugins point here instead of each re-explaining the dual path.",
      "version": "0.1.0",
      "category": "workflow",
      "keywords": [
        "github",
        "gh-cli",
        "github-mcp",
        "azure-pipelines",
        "ci",
        "pull-request",
        "reference",
        "dual-path"
      ]
    },
    {
      "name": "loop-dispatch",
      "source": "./plugins/loop-dispatch",
      "description": "Single front door for a repo's automation loops so one routine per repo can handle every loop event instead of one routine per event. Deterministically routes each triggering GitHub event to the matching loop (via route-event.sh); a non-matching event is a quiet no-op. Router only; each loop owns its guardrails. Also bundles new-loop-routine — the single source of truth for standing up a standardised loop routine per repo (config + canonical prompts + UI wiring steps). Repo-agnostic; requires github-ops and the loop skills it routes to.",
      "version": "0.1.0",
      "category": "workflow",
      "keywords": [
        "routine",
        "dispatch",
        "router",
        "github-events",
        "cross-repo",
        "automation",
        "cloud-routine"
      ]
    },
    {
      "name": "ops-capabilities",
      "source": "./plugins/ops-capabilities",
      "description": "The framework-default capability skills a consumer inherits: ops-integrate (land an approved PR — owns every merge gate and the release-base skip), ops-branching (the repo's branch model, with the integration branches, release base and merge strategy PRIVATE — callers ask for outcomes, never values), ops-ci (CI status + failing-build logs, read-only, provider internal), ops-repo-meta (ambient repo facts as data: identity, topology, live lines), and ops-notify (tell a human). Each is a skill named ops-<capability>, invoked by name with (action, context-json) per catalog.json; a repo overrides one by shipping its own skill of the same name. The two capabilities that are ALWAYS the repo's — ops-change and ops-release — are deliberately absent.",
      "version": "0.1.0",
      "category": "workflow",
      "keywords": [
        "capability",
        "framework-default",
        "merge",
        "branching",
        "ci",
        "repo-meta",
        "notify"
      ]
    },
    {
      "name": "ops-merge-loop",
      "source": "./plugins/ops-merge-loop",
      "description": "The landing loop. Sweeps the PRs a maintainer has labelled `ops/auto-merge` — the deliberate go-signal that stands in for a review approval, since you cannot approve your own PR — and hands each to `ops-integrate · land`, then comments the outcome. It owns scheduling only: the sweep, the CI poll cadence and its 15-minute cap, the cap of 10 PRs per run, and the report. Every merge gate, the release-base skip and the merge itself belong to `ops-integrate`, so this loop never resolves a base branch or a merge strategy and never calls `ops-branching`. Uses /goal so a landing job is provably finished. Repo-agnostic; runs locally or as a scheduled cloud routine. Requires ops-capabilities and github-ops.",
      "version": "0.2.0",
      "category": "workflow",
      "keywords": [
        "pull-request",
        "merge",
        "landing",
        "ci",
        "guardrail",
        "automation",
        "cloud-routine"
      ]
    },
    {
      "name": "ops-release-loop",
      "source": "./plugins/ops-release-loop",
      "description": "The release loop: issue-triggered, CI-gated, no mid-flow human approval. Labelling a `release <version>` issue `ops/auto-release` makes it command the repo's `ops-release` through plan → cut → publish → sync, driving CI green in between and gating on an Opus pre-publish review against a repo-overridable checklist (a BLOCK files an issue, notifies, and de-labels rather than shipping). Every release mechanic — version files, changelog, tagging, publish target, back-merge — belongs to `ops-release`, which is ALWAYS repo-provided; this loop only orchestrates and gates, and never calls `ops-branching`. Bundles the release-reviewer agent, the default review checklist, and the `sync` contract. Requires ops-capabilities and github-ops.",
      "version": "0.2.0",
      "category": "workflow",
      "keywords": [
        "release",
        "tagging",
        "publish",
        "ci-gate",
        "pre-publish-review",
        "cloud-routine"
      ]
    }
  ]
}
